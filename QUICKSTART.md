# MyWallet - Быстрый старт

## Что создано

Полнофункциональный Telegram-бот для учета личных финансов с AI интеграцией.

### Структура проекта:

```
MyWallet/
├── bot/
│   ├── main.py                          ✅ Главный файл бота
│   ├── handlers/
│   │   ├── start_handler.py            ✅ /start и /help
│   │   ├── transaction_handler.py       ✅ Добавление транзакций
│   │   ├── voice_handler.py            ✅ Голосовые сообщения
│   │   ├── photo_handler.py            ✅ Распознавание чеков
│   │   ├── stats_handler.py            ✅ Статистика
│   │   └── export_handler.py           ✅ Экспорт в Excel
│   └── keyboards/
│       └── inline_keyboards.py         ✅ Клавиатуры
├── services/
│   ├── database.py                     ✅ Supabase клиент
│   ├── ai_service.py                   ✅ GPT-4 интеграция
│   ├── voice_service.py                ✅ Whisper API
│   ├── vision_service.py               ✅ GPT-4 Vision для чеков
│   ├── categorization_service.py       ✅ Автокатегоризация
│   └── export_service.py               ✅ Excel генератор
├── models/
│   ├── transaction.py                  ✅ Модель транзакции
│   └── category.py                     ✅ Модель категории
├── utils/
│   ├── config.py                       ✅ Конфигурация
│   └── helpers.py                      ✅ Вспомогательные функции
├── data/
│   └── default_categories.json         ✅ Категории
├── database/
│   └── setup.sql                       ✅ SQL скрипт для Supabase
├── data_migration.py                   ✅ Импорт заметок
├── requirements.txt                    ✅ Зависимости
├── .env.example                        ✅ Пример конфигурации
├── .gitignore                          ✅
└── README.md                           ✅ Документация
```

## Следующие шаги

### 1. Настройка Supabase (5 минут)

1. Зарегистрируйтесь на [supabase.com](https://supabase.com)
2. Создайте новый проект
3. Выполните SQL из `database/setup.sql` в **SQL Editor**
4. Создайте bucket `receipts` в **Storage** (сделайте публичным)
5. Скопируйте URL и Key из **Settings > API**

### 2. Настройка OpenAI API (3 минуты)

1. Зарегистрируйтесь на [platform.openai.com](https://platform.openai.com)
2. Создайте API ключ в [API Keys](https://platform.openai.com/api-keys)
3. Пополните баланс ($5-10)

### 3. Создание Telegram бота (2 минуты)

1. Откройте [@BotFather](https://t.me/BotFather)
2. Отправьте `/newbot` и следуйте инструкциям
3. Получите **токен бота**
4. Отправьте `/setprivacy` → **Disable**
5. Получите свой ID через [@userinfobot](https://t.me/userinfobot)

### 4. Настройка окружения (2 минуты)

Скопируйте `.env.example` в `.env`:

```bash
cp .env.example .env
```

Заполните ваши данные в `.env`:
- `TELEGRAM_BOT_TOKEN` - токен от BotFather
- `SUPABASE_URL` - URL вашего проекта Supabase
- `SUPABASE_KEY` - anon public key из Supabase
- `OPENAI_API_KEY` - ключ от OpenAI
- `USER_TELEGRAM_ID` - ваш Telegram ID

### 5. Установка зависимостей (2 минуты)

```bash
# Создать виртуальное окружение
python3 -m venv venv

# Активировать
source venv/bin/activate  # Mac/Linux
# или
venv\Scripts\activate  # Windows

# Установить зависимости
pip install -r requirements.txt
```

### 6. Запуск бота

```bash
python -m bot.main
```

Вы должны увидеть:
```
INFO - Starting MyWallet bot...
```

Откройте Telegram, найдите вашего бота и отправьте `/start`.

## Миграция существующих данных

У вас уже есть записи о финансах в тексте. Чтобы их импортировать:

1. Скопируйте ваши заметки в файл `my_notes.txt`
2. Запустите:

```bash
python data_migration.py
```

Скрипт использует GPT-4 для автоматического парсинга ваших заметок.

**Ваши заметки из сообщения:**
```
Расход - приход
Где написано -… означает трата
Остальное объясняю

3д модели :
42 000 получили
...
(весь текст ваших заметок)
```

## Примеры использования

### Добавление транзакции текстом:
Просто напишите боту:
```
Потратил 500 на такси
```

### Голосовое сообщение:
Запишите: "Купил продукты за тысячу двести гривен"

### Фото чека:
Отправьте фото → бот извлечет сумму и товары

### Статистика:
`/stats` → Выберите период

### Экспорт:
`/export` → Получите Excel файл

## Технические детали

### AI функции:
- **GPT-4**: Категоризация, парсинг NLP, рекомендации
- **Whisper**: Распознавание голоса
- **GPT-4 Vision**: Сканирование чеков

### База данных:
- **Supabase (PostgreSQL)**: Облачное хранение
- **Таблицы**: transactions, categories, ai_recommendations
- **Storage**: Фото чеков

### Безопасность:
- Single-user режим (только ваш Telegram ID)
- Все ключи в `.env` (не коммитятся в git)
- Row Level Security в Supabase

## Стоимость

При умеренном использовании (~50 транзакций/день):
- **Supabase**: Бесплатно
- **OpenAI**: ~$5-10/месяц

## Поддержка

Если что-то не работает:

1. Проверьте `.env` - все ли ключи указаны
2. Проверьте, что выполнили `database/setup.sql` в Supabase
3. Проверьте баланс OpenAI
4. Проверьте логи бота - там будут ошибки
5. Посмотрите README.md - раздел Troubleshooting

## Что дальше?

После запуска бота вы можете:

1. **Импортировать старые данные** через `data_migration.py`
2. **Добавить первые транзакции** через бота
3. **Посмотреть статистику** `/stats`
4. **Экспортировать в Excel** `/export`
5. **Получить AI рекомендации** кнопка "🤖 AI рекомендации"

---

**Проект готов к использованию!** 🎉

Все файлы созданы, код написан, осталось только настроить API ключи и запустить.
