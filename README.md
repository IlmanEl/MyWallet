# MyWallet - AI-Powered Personal Finance Telegram Bot

Персональный Telegram-бот для учета финансов с интеграцией искусственного интеллекта.

## Возможности

- 💸 **Учет доходов и расходов** с автоматической категоризацией
- 🎤 **Распознавание голосовых сообщений** через Whisper API
- 📸 **Сканирование чеков** с помощью GPT-4 Vision
- 🤖 **AI категоризация** транзакций через GPT-4
- 📝 **Обработка естественного языка**: просто напишите "Потратил 500 на такси"
- 📊 **Детальная статистика** по дням, неделям, месяцам, годам
- 📤 **Экспорт в Excel** с разбивкой по категориям
- 💡 **Персональные финансовые рекомендации** от AI
- ☁️ **Облачное хранение** на Supabase

## Технологии

- **Backend**: Python 3.11+
- **Bot Framework**: python-telegram-bot 20.x
- **Database**: Supabase (PostgreSQL)
- **AI**: OpenAI (Whisper, GPT-4, GPT-4 Vision)
- **Export**: openpyxl, pandas, matplotlib

## Установка

### 1. Клонирование и зависимости

```bash
cd MyWallet

# Создать виртуальное окружение
python -m venv venv

# Активировать (Mac/Linux)
source venv/bin/activate

# Активировать (Windows)
venv\Scripts\activate

# Установить зависимости
pip install -r requirements.txt
```

### 2. Создание Telegram бота

1. Откройте [@BotFather](https://t.me/BotFather) в Telegram
2. Отправьте команду `/newbot`
3. Следуйте инструкциям и получите **токен бота**
4. Отправьте `/setprivacy` и выберите **Disable** (чтобы бот видел все сообщения)
5. Получите свой Telegram User ID через [@userinfobot](https://t.me/userinfobot)

### 3. Настройка Supabase

1. Зарегистрируйтесь на [supabase.com](https://supabase.com)
2. Создайте новый проект
3. Перейдите в **SQL Editor** и выполните скрипт из `database/setup.sql`
4. Перейдите в **Storage** и создайте bucket с именем `receipts`, сделайте его публичным
5. В **Settings > API** скопируйте:
   - **Project URL** (SUPABASE_URL)
   - **anon public key** (SUPABASE_KEY)

### 4. Получение OpenAI API ключа

1. Зарегистрируйтесь на [platform.openai.com](https://platform.openai.com)
2. Перейдите в [API Keys](https://platform.openai.com/api-keys)
3. Создайте новый ключ
4. **Важно**: Пополните баланс ($5-10 для начала)

### 5. Настройка переменных окружения

Создайте файл `.env` на основе `.env.example`:

```bash
cp .env.example .env
```

Заполните `.env` вашими данными:

```env
TELEGRAM_BOT_TOKEN=123456:ABC-DEF1234ghIkl-zyx57W2v1u123ew11
SUPABASE_URL=https://xxxxxxxxxxxxx.supabase.co
SUPABASE_KEY=eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...
OPENAI_API_KEY=sk-proj-...
USER_TELEGRAM_ID=123456789
ENVIRONMENT=production
```

## Запуск бота

```bash
python -m bot.main
```

Если все настроено правильно, вы увидите:

```
INFO - Starting MyWallet bot...
```

Откройте Telegram, найдите вашего бота и отправьте `/start`.

## Использование

### Добавление транзакций

#### 1. Текстом (NLP)
Просто напишите боту:
- "Потратил 500 на такси"
- "Получил 10000 зарплата"
- "Купил продукты за 1200"

#### 2. Голосовым сообщением
Запишите голосовое: "Потратил триста гривен на кафе с друзьями"

#### 3. Фото чека
Отправьте фото чека из магазина - бот автоматически извлечет сумму, дату и товары

#### 4. Кнопками
Используйте главное меню для структурированного ввода

### Команды

- `/start` - Главное меню
- `/balance` - Текущий баланс
- `/stats` - Статистика
- `/export` - Экспорт в Excel
- `/help` - Справка

### Просмотр статистики

Нажмите "📊 Статистика" и выберите период:
- День
- Неделя
- Месяц
- Год

Бот покажет:
- Общие доходы и расходы
- Топ категорий расходов
- Топ категорий доходов
- Процентное распределение

### Экспорт данных

1. Нажмите "📤 Экспорт Excel"
2. Выберите период
3. Получите Excel файл с тремя листами:
   - Все транзакции
   - Статистика по категориям
   - Месячная сводка

### AI Рекомендации

Нажмите "🤖 AI рекомендации" для получения персональных советов по оптимизации бюджета на основе ваших трат за последние 30 дней.

## Миграция существующих данных

Если у вас есть записи о финансах в заметках, вы можете импортировать их:

1. Скопируйте ваши заметки в файл `my_notes.txt`
2. Запустите скрипт миграции:

```bash
python data_migration.py
```

Скрипт использует GPT-4 для парсинга ваших заметок и создания транзакций в базе данных.

## Структура проекта

```
MyWallet/
├── bot/
│   ├── main.py                    # Точка входа
│   ├── handlers/                  # Обработчики команд
│   └── keyboards/                 # Клавиатуры
├── services/
│   ├── database.py               # Supabase интеграция
│   ├── ai_service.py             # OpenAI GPT-4
│   ├── voice_service.py          # Whisper
│   ├── vision_service.py         # GPT-4 Vision
│   ├── categorization_service.py # Автокатегоризация
│   └── export_service.py         # Excel экспорт
├── models/                       # Модели данных
├── utils/                        # Утилиты
├── data/                         # Категории по умолчанию
├── database/                     # SQL скрипты
└── requirements.txt
```

## Категории

### Расходы
- 🍔 Еда и напитки (Рестораны, Продукты, Кафе)
- 🚗 Транспорт (Такси, Автобусы, Бензин)
- 🏥 Здоровье (Аптеки, Врачи, Анализы)
- 🏠 Дом (Коммунальные, Ремонт, Мебель)
- 💼 Работа/Бизнес (Материалы, Инструменты, Партнерам)
- 📚 Образование (Курсы, Книги)
- 🎮 Развлечения
- 👕 Одежда
- 🎁 Подарки
- 💸 Переводы

### Доходы
- 💰 Зарплата
- 💼 Проекты (3D модели, AI агенты, Фриланс)
- 🎁 Подарки
- 📈 Инвестиции

## Безопасность

- Все API ключи храните в `.env` (не коммитьте!)
- Используйте Row Level Security в Supabase
- Бот работает в single-user режиме (только для вашего Telegram ID)
- Регулярно делайте бэкапы базы данных Supabase

## Стоимость использования

Примерная стоимость при умеренном использовании (~50 транзакций в день):

- **Supabase**: Бесплатно (Free tier до 500MB)
- **OpenAI API**: ~$5-10/месяц
  - GPT-4: $0.01 за 1k tokens (категоризация, NLP)
  - Whisper: $0.006 за минуту (голосовые сообщения)
  - GPT-4 Vision: $0.01 за изображение (чеки)

## Troubleshooting

### Бот не отвечает
1. Проверьте, что бот запущен (`python -m bot.main`)
2. Проверьте токен бота в `.env`
3. Проверьте, что ваш User ID указан в `USER_TELEGRAM_ID`

### Ошибки с базой данных
1. Убедитесь, что выполнили `database/setup.sql` в Supabase
2. Проверьте SUPABASE_URL и SUPABASE_KEY
3. Проверьте RLS политики в Supabase

### OpenAI API ошибки
1. Проверьте, что ключ API валиден
2. Убедитесь, что есть баланс на аккаунте OpenAI
3. Проверьте лимиты API

### Не работает распознавание голоса/чеков
1. Проверьте баланс OpenAI
2. Убедитесь, что используете правильные модели (whisper-1, gpt-4-vision-preview)
3. Проверьте размер файлов (не более 20MB для голоса, 20MB для фото)

## Деплой (опционально)

### На VPS (Ubuntu)

```bash
# Установить зависимости
sudo apt update
sudo apt install python3.11 python3-pip python3-venv

# Клонировать и настроить проект
git clone <your-repo>
cd MyWallet
python3 -m venv venv
source venv/bin/activate
pip install -r requirements.txt

# Создать .env
nano .env

# Запустить в фоне с помощью systemd
sudo nano /etc/systemd/system/mywallet.service
```

Содержимое `mywallet.service`:

```ini
[Unit]
Description=MyWallet Telegram Bot
After=network.target

[Service]
Type=simple
User=your-username
WorkingDirectory=/path/to/MyWallet
Environment="PATH=/path/to/MyWallet/venv/bin"
ExecStart=/path/to/MyWallet/venv/bin/python -m bot.main
Restart=always

[Install]
WantedBy=multi-user.target
```

Запустить:

```bash
sudo systemctl daemon-reload
sudo systemctl enable mywallet
sudo systemctl start mywallet
sudo systemctl status mywallet
```

## Roadmap

- [ ] Multi-user support
- [ ] Семейные/совместные бюджеты
- [ ] Telegram Web App для расширенной статистики
- [ ] Интеграция с банковскими API
- [ ] Уведомления о превышении бюджета
- [ ] Графики и визуализация в чате

## License

MIT

## Автор

Создано для личного использования с помощью Claude Code.

---

Если возникли вопросы или проблемы, создайте issue в репозитории.
